'use client';

import { CloudDownloadIcon } from 'lucide-react';

const Loading = () => {
    return (
        <CloudDownloadIcon className='size-15 text-yellow-500 animate-bounce mt-10 mx-auto' />
    );
};

export default Loading;